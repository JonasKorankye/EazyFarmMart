<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.farmmart.jonas.eazyfarmmart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.SignupFarmerActivity" />
        <activity android:name=".activity.ResetPasswordActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.PrivacyPolicyActivity" />
        <activity android:name=".activity.SignupConsumerActivity" />
        <activity android:name=".activity.HomeConsumerActivity" />
        <activity android:name=".activity.ViewFarmProduce" />
        <activity android:name=".activity.FindFarmerActivity" />
        <activity android:name=".activity.ViewFarmProduceByConsumer"
            android:exported="true"/>
        <activity android:name=".activity.UploadFarmActivity" />
        <activity android:name=".activity.ShowImagesActivity" />
    </application>

</manifest>